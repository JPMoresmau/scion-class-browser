name:           scion-browser
version:        0.1
cabal-version:  >= 1.2
build-type:     Simple

executable hoogle-parsec
  hs-source-dirs:  src
  main-is:         Main.hs
  build-depends:   base, text, containers,
                   parsec >= 3,
                   cereal, derive, bytestring,
                   directory, filepath,
                   Cabal, haskell-src-exts,
                   process, unix,
                   deepseq

